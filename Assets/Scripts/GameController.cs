using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameController : MonoBehaviour
{
    // Need to keep track of point system

    // Need to keep track of life system

    // Need to keep track of spawn location

    // Need to keep track of turtle

    // Need to keep track of plastic bag and jellyfish models

    // Start is called before the first frame update
    void Start()
    {
        // initialize a couple of plastic bags and jellyfish
        // put the turtle in the middle
    }

    // Update is called once per frame
    void Update()
    {
        // Decrease the life system by a certain amount
        // Increase point system by a certain amount
    }

    // Need to create a collision event

    // if a jellyfish and turtle collide, then increase life system

    // if a plastic bag and turtle collide, game over, get the game over screen (with description) and a play or return to home button
}
